spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/echome
    username: root
    password: ${DB_PWD}
#
#  h2:
#    console:
#      path: /h2-console
#      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    #    generate-ddl: true
#    defer-datasource-initialization: true #data.sql ???? ?? ??
#  sql:
#    init:
#      mode: always #data.sql ???? ?? ??
  config:
    import: application-jwt.yml
#  config:
#    import: application-key.yml


